# details 关键词提取规范（风格B v3——核心标签、语义原子化）

> 适用对象：`core_memory0720.csv` 去重后的关键词提取切片  
> 使用角色：关键词提取子 Agent  
> 规范地位：本文件是逐行语义判断的唯一完整规则来源

---

## 1. 规范目标与核心原则

本任务不是摘要，也不是从原句中截取尽可能完整的短语，而是把 `details` 转换成适合检索、推荐和画像使用的**最小充分语义标签**。

关键词提取必须遵循以下要求：

1. 正向偏好优先保留核心对象；`看、听、吃、玩、观看、练习、收藏` 等仅用于引出对象时应删除。
2. `朋友、同学、女儿、父亲` 等关系锚点属于核心信息，必须按关系类规则显式输出。
3. `一个、一只、一台` 等无区分价值的弱数量词，以及 `相关内容、综艺节目、的电影` 等冗余外壳应删除。
4. 地点层级、时间与事件等存在依存关系时，应组成完整语义单元，不能拆成失去上下文的碎片。
5. `计划、要求、关注、推测、可能、曾经` 等成分仅承担叙述、意图或证据引导作用时应删除；其本身构成核心事实时才保留。
6. 允许在不改变事实和极性的前提下进行必要的结构压缩，例如将 `关于机器人的电影` 提取为 `机器人电影`。

核心原则是：

> **保真对象从“原句措辞”改为“核心事实与极性”；允许删除语法外壳并进行轻量结构压缩，但不得编造新事实、改变专名、丢失否定或关键限定。**

---

## 2. 输出目标：最小充分语义标签

合格关键词必须满足：

- **核心化**：直接表达人物、实体、对象、属性、状态、关系、地点、时间或事件核心。
- **精简化**：删除用户主语、态度引导、承载动作、叙述外壳和弱语气。
- **可检索**：优先输出可用于画像匹配的词，而不是完整句法成分。
- **不反义**：否定、拒绝、没有、禁止、失败等极性必须保留。
- **依存完整**：不能把必须相互依赖的修饰语拆成无意义碎片。
- **不编造**：只允许结构压缩，不允许凭常识新增事实。

### 2.1 判断优先级

规则冲突时按以下优先级处理：

1. 不新增原文不存在的事实。
2. 不改变肯定、否定、拒绝、缺失、失败等语义极性。
3. 保留专名、数字、时间、地点、对象、状态和关系中的核心信息。
4. 删除叙述外壳和无独立检索价值的动作、语气、类别套话。
5. 将独立语义拆开，将强依存成分合并。
6. 去重并统一输出格式。

不确定是否删除某个成分时，使用以下判断：

> **删除这部分后，核心事实、极性或区分度是否变化？不变化则删除，变化则保留。**

---

## 3. 强制执行的两阶段提取法

每行必须先理解事实，再压缩为标签，不能边读边机械删词。

### 阶段一：建立语义草图

1. 结合 `topic`、`subtopic` 判断业务类别。
2. 按事实而不是按标点切分分句。
3. 标记主实体、关系实体、核心对象、语义极性、时间、地点、数量、属性、状态、结果和专名。
4. 识别只负责叙述的外壳，如 `用户表示、喜欢、想、计划、要求、认为、提到、推测、关注、正在、曾经`。

### 阶段二：生成关键词

1. 删除用户主语、系词、连接词和无独立价值的叙述外壳。
2. 正向偏好优先保留对象，删除纯承载动作。
3. 否定事实保留否定谓词和对象。
4. 关系类事实补出关系锚点。
5. 删除弱量词、冗余类别套话和重复上位词。
6. 进行轻量结构压缩，使标签短而完整。
7. 按“独立则拆、依存则合”决定边界。
8. 去重后使用 `, ` 连接。

---

## 4. 规则1：正向偏好提取对象，不保留承载动作

### 4.1 默认删除的正向引导与承载动作

下列词仅用于引出偏好对象、观看对象、学习对象或收藏对象时应删除：

`喜欢、偏好、最爱、爱、想、希望、要、比较喜欢、尤其喜欢、感兴趣、关注、收藏、单曲循环、看、观看、听、唱、吃、喝、玩、打、做、学、学习、练习、阅读、追、参加、了解、欣赏`

这不是全局字符串替换。只有删除后仍能完整表达核心对象时才删除。

| details | ✗ 旧式结果 | ✓ v3 结果 |
|---|---|---|
| 用户喜欢看电视剧 | 看电视剧 | 电视剧 |
| 用户最爱看科幻片 | 看科幻片 | 科幻片 |
| 用户喜欢吃辣 | 吃辣 | 辣 |
| 用户想观看2026年美加墨世界杯 | 观看2026年美加墨世界杯 | 2026年美加墨世界杯 |
| 用户喜欢练习八段锦 | 练习八段锦 | 八段锦 |
| 用户收藏了居家燃脂健身课程 | 收藏了居家燃脂健身课程 | 居家燃脂健身课程 |
| 用户有单曲循环《刺》的习惯 | 单曲循环刺 | 刺 |
| 用户喜欢听《青衣》和邓紫棋的歌曲 | 听青衣, 邓紫棋 | 青衣, 邓紫棋 |
| 用户想学习画画 | 学习画画 | 画画 |

### 4.2 动作何时保留

动作本身是行为、习惯、事件或状态，而不是对象引导词时，保留动作核心，但删除时态助词和叙述套话。

- `每天跑步5公里`：保留 `每天跑步5公里`。
- `晚上听歌，低音少一点`：`晚上听歌` 是场景习惯，可保留。
- `2026-05-30一起去爬了山` → `2026-05-30, 爬山`。
- `每4小时给紫玫瑰浇一次水` → `每4小时, 紫玫瑰, 浇水`。

判断问题：删除动作后是否只剩清晰偏好对象？是则删除。删除后是否丢失“做了什么”的核心事实？是则保留动作核心。

---

## 5. 规则2：否定、拒绝、缺失和失败必须保留

含 `不、没、没有、无、非、不爱、不吃、不要、拒绝、讨厌、忌、戒、失败、无法、不会、不见` 等极性时：

1. 必须保留极性。
2. 必须保留被否定或失败的对象。
3. 不得把负向偏好压缩成正向对象。
4. 可删除不影响极性的程度副词、叙述主语和重复原因对象。

| details | ✗ 错误 | ✓ 正确 |
|---|---|---|
| 不喜欢恐怖片 | 恐怖片 | 不喜欢恐怖片 |
| 不爱看灵异类小说 | 灵异类小说 | 不爱看灵异类小说 |
| 不喜欢打桌球，觉得太吵 | 桌球, 太吵 | 不喜欢打桌球, 太吵 |
| 不想通过节食来减肥，因为容易反弹 | 节食减肥, 容易反弹 | 不想通过节食来减肥, 容易反弹 |
| 不喜欢当前播放的歌曲，想换《单身情歌》 | 当前播放歌曲, 换单身情歌 | 不喜欢当前播放的歌曲, 单身情歌 |
| 遥控器不见了 | 遥控器, 不见 | 遥控器不见了 |
| 变形器不会变形 | 变形器, 变形 | 变形器不会变形 |

否定原因的主语只有在缺失后会造成歧义时才补足：

- `不喜欢超级英雄电影，觉得太幼稚` → `不喜欢超级英雄电影, 超级英雄电影幼稚`。
- `不喜欢去医院，认为太麻烦` → `不喜欢去医院, 太麻烦`，上下文已清楚时不重复对象。

---

## 6. 规则3：关系类必须保留关系锚点

`topic=社交关系` 或内容明确表达亲属、朋友、同学、同事等关系时，关系词不是可删除类别，而是核心实体。姓名、称谓、属性和事件应与关系锚点分别提取。

| details | ✗ 错误 | ✓ 正确 |
|---|---|---|
| 用户的朋友是安德烈 | 安德烈 | 朋友, 安德烈 |
| 用户的同学是诸葛双双 | 诸葛双双 | 同学, 诸葛双双 |
| 用户的哥哥叫通通 | 通通 | 哥哥, 通通 |
| 用户有一个女儿 | 一个女儿 | 女儿 |
| 用户女儿的出生日期是2016年8月8日 | 女儿, 出生日期2016年8月8日 | 女儿, 2016年8月8日 |
| 用户的父亲是做外贸的 | 父亲, 做外贸 | 父亲, 外贸 |
| 用户的嫂子的同事叫婷婷 | 嫂子的同事, 婷婷 | 嫂子, 同事, 婷婷 |

补充规则：

- `subtopic` 能帮助确定规范关系名。`闺女` 在 `subtopic=女儿` 下可输出 `女儿`。
- `小女儿、大孩子、工位对面同事` 等具有区分作用的限定关系应保留。
- 一条内容有多层关系时拆开关系链中的有效实体，例如 `嫂子, 同事, 婷婷`。
- 不能因为关系词与 `subtopic` 相同就删除关系锚点。

---

## 7. 规则4：删除语法外壳、弱量词和字段套话

### 7.1 删除用户叙述外壳

通常删除：

`用户、用户的、表示、明确表示、提到、说、认为、觉得、询问是否、希望、计划、准备、要求、明确要求、关注、推测为、可能、曾经、目前、现在、最近、已经、会、要、了、进行、存在、有……的习惯、有……的意愿、……问题、……状态`

但如果时间或状态本身有独立区分价值，必须保留核心部分：

- `最近一次体检在半年前` → `半年前, 体检`。
- `已找到遥控器` → `遥控器, 已找到`。
- `2026-07-10丢失` 中日期和丢失状态都保留。

### 7.2 删除弱量词，保留有效数量

无区分价值的存在量词通常删除：`一个、一只、一台、一些、一座、一把`。

- `一只不听话的乌龟` → `乌龟, 不听话`。
- `一个女儿` → `女儿`。
- `一台联想拯救者笔记本` → `联想拯救者笔记本`。

有效数量、金额、尺寸、频率和编号必须保留：

- `两只小猫状态一直不好` → `两只小猫状态一直不好`。
- `20万元黄金`、`第1层`、`宽3厘米`、`每天一包烟` 均保留。

### 7.3 删除字段类别和关系套话

当 `subtopic` 或相邻实体已经说明字段含义时，删除 `名字、昵称、手机号、出生日期、体重、身高、品牌、地址是、家在、老家在` 等字段外壳，只保留值。

- `昵称是芳华` → `芳华`。
- `女儿的出生日期是2016年8月8日` → `女儿, 2016年8月8日`。
- `用户家在河北，老家在山东` → `河北, 山东`。
- `机顶盒是华为品牌` → `机顶盒, 华为`。

关系锚点不属于字段外壳，不能按本规则删除。

---

## 8. 规则5：允许轻量结构压缩，不允许事实改写

v3 允许把冗长句法压缩成更自然的标签结构：

1. 删除 `的、了、是、在、有、进行、相关、类型、方面` 等虚词或套话。
2. 将“关于 X 的 Y”压缩为 `XY`。
3. 将属性前置或后置，形成常见名词短语。
4. 删除专名后的冗余载体词。
5. 将动作结果压缩为状态或对象标签。

| 原文片段 | ✓ 结构压缩 |
|---|---|
| 关于机器人的电影 | 机器人电影 |
| 三岁儿童的电影动画片 | 三岁儿童动画片 |
| 动画版的《三国演义》 | 三国演义动画版 |
| 流行的歌 | 流行歌曲 |
| 豆浆必须是甜的 | 甜豆浆 |
| 一到冬天就起皮 | 冬天起皮 |
| 用于助眠 | 助眠 |
| 身上还有皮肤病 | 皮肤病 |
| 推测为湖北江陵出土的越王勾践剑 | 湖北江陵, 越王勾践剑 |

禁止：改错人名、作品名或品牌名；补充原文没有的职业、类型、因果或诊断；改变语义极性；为追求书面化随意替换专名、日期、金额或编号。

轻量结构压缩不是随意改写的许可。没有明确必要时，保留原文中的专名和数值表达。

---

## 9. 规则6：命名实体与泛化类别的取舍

### 9.1 命名实体删除冗余载体词

作品名、节目名、游戏名或人名已经明确时，通常删除其后的冗余载体词：

- `奔跑吧综艺节目` → `奔跑吧`。
- `战狼电影` → `战狼`。
- `植物大战僵尸游戏` → `植物大战僵尸`。
- `小猪佩奇相关的影视内容` → `小猪佩奇`。
- `刘德华的电影` → `刘德华`。

### 9.2 泛化类别本身是偏好对象时保留

- `科幻电影`、`经典老歌`、`电视剧`、`美剧`、`枪战片`、`有氧运动` 是可独立检索的类别，应保留。
- 上位类别仅用于引出具体示例且没有独立新增信息时可删除。例如已列出 `老友记, 生活大爆炸` 时，介绍性的 `美剧` 可不重复输出。

### 9.3 人名、作品、版本和属性按语义拆分

- `花粥的《盗将行》` → `花粥, 盗将行`。
- `《我们背对背拥抱》的DJ版本` → `我们背对背拥抱, DJ版本`。
- `刘德华主演的《赌神》` → `刘德华, 赌神`。

不能把不同命名实体粘成一个关键词。

---

## 10. 规则7：独立则拆，依存则合

逗号不是越多越好。先判断两个片段能否各自独立检索，以及拆分是否会丢失修饰关系。

### 10.1 必须拆分

- 不同人物、作品、品牌、物品、地点、金额或独立状态。
- 人名与作品名：`张学友, 吻别`。
- 物品与位置：`钥匙, 鞋柜`。
- 关系与姓名：`朋友, 安德烈`。
- 来源地与目的地：`通州, 朝阳`。

### 10.2 必须合并

拆开后会失去依存关系的内容应形成最小完整单元：

- `宽3厘米`，不能写成 `宽, 3厘米`。
- `第1层`，不能写成 `第1, 层`。
- `白色小狗`，不能机械写成 `白色, 小狗`。
- `电视今天无法关机`，不能写成 `电视, 今天, 无法关机`。
- `两只小猫状态一直不好`，不能写成 `两只小猫, 状态一直不好`。
- `2026-05-08打疫苗`，时间只修饰该事件时应合并。

### 10.3 地点层级

同一条连续位置路径通常保持为一个关键词：

- `药放在客厅左手边的抽屉第1层` → `药, 客厅左手边抽屉第1层`。
- `住在阳光花园小区3号楼202室` → `阳光花园小区3号楼202室`。

原文将地点作为多个独立实体强调，或表达来源地与目的地时拆分：

- `从通州搬到朝阳` → `通州, 朝阳`。
- `药放在卧室床头柜的第2层`：如果各位置层级均需独立检索，可输出 `药, 卧室, 床头柜, 第2层`；否则优先保持连续位置路径。

地点粒度出现冲突时，优先避免产生失去上下文的孤立碎片。

### 10.4 时间与事件

- 时间是独立画像信息时可拆分：`张伟, 2026-05-30, 爬山`。
- 时间只用于限定单一事件时优先合并：`2026-05-08打疫苗`、`每天6:20闹钟`。
- 不要同时输出一个孤立时间和一个重复带时间的事件。

---

## 11. 规则8：属性、状态、原因与结果

1. 属性能独立检索且对象清楚时可以拆分：`乌龟, 不听话`。
2. 属性与对象高度绑定时合并：`白色小狗`、`甜豆浆`。
3. 多个属性描述同一实体时，先输出实体，再输出属性：`小狗狗, 球球, 灰黑色毛发, 特别黏人, 大眼睛`。
4. 原因句删除重复主语：`花生过敏, 起疹子`，不写 `吃花生会起疹子`。
5. 结果状态去除弱语气：`可能存在中毒风险` → `中毒风险`。
6. 只保留有画像价值的原因或结果，纯叙事过程可删除。

---

## 12. 规则9：去重、上位词与信息覆盖

- 同一事实只输出一次，不因原文重复表达而重复关键词。
- 具体实体已经完整覆盖介绍性上位词时，删除冗余上位词。
- 上位类别本身是独立偏好时保留。
- `topic` 和 `subtopic` 只用于帮助理解，不能直接无条件复制到关键词。
- 关系类 `subtopic` 是例外：关系锚点通常需要显式输出。

示例：

- `偏好美剧，如《老友记》和《生活大爆炸》` → `老友记, 生活大爆炸`。
- `喜欢看美剧` → `美剧`。
- `喜欢刘德华的电影，也想看刘德华主演的作品` → `刘德华`。

---

## 13. 综合提取示例

| topic | subtopic | details | keywords |
|---|---|---|---|
| 个人身份 | 手机号 | 用户明确表示不希望使用以199开头的手机号 | 不使用199开头手机号 |
| 个人身份 | 家庭住址 | 用户家在河北，老家在山东 | 河北, 山东 |
| 个人身份 | 家庭住址 | 用户住在阳光花园小区3号楼202室 | 阳光花园小区3号楼202室 |
| 兴趣爱好 | 电影偏好 | 用户喜欢看奥特曼 | 奥特曼 |
| 兴趣爱好 | 电影偏好 | 用户经常看宋雨琦出演的奔跑吧综艺节目 | 宋雨琦, 奔跑吧 |
| 兴趣爱好 | 电影偏好 | 用户喜欢关于机器人的电影 | 机器人电影 |
| 兴趣爱好 | 音乐偏好 | 用户有单曲循环《刺》的习惯，并且也喜欢《全世界，我最美》 | 刺, 全世界，我最美 |
| 兴趣爱好 | 饮食偏好 | 用户喜欢吃辣 | 辣 |
| 兴趣爱好 | 书籍偏好 | 用户喜欢看道士类小说，不爱看灵异类小说 | 道士类小说, 不爱看灵异类小说 |
| 兴趣爱好 | 运动偏好 | 用户喜欢周末去野钓，使用手竿，最喜欢钓鲫鱼 | 周末, 野钓, 手竿, 鲫鱼 |
| 社交关系 | 朋友 | 用户的朋友是安德烈 | 朋友, 安德烈 |
| 社交关系 | 女儿 | 用户女儿的出生日期是2016年8月8日 | 女儿, 2016年8月8日 |
| 物品管理 | 物品位置 | 用户的药放在客厅左手边的抽屉第1层 | 药, 客厅左手边抽屉第1层 |
| 物品管理 | 物品位置 | 用户的母亲找到了遥控器 | 母亲, 遥控器, 已找到 |
| 健康生活 | 健康情况 | 用户对花生过敏，吃了会起疹子 | 花生过敏, 起疹子 |
| 健康生活 | 健康情况 | 用户最近一次体检是在半年前，血脂偏高一点点 | 半年前, 体检, 血脂偏高 |
| 旅行出行 | 旅游偏好 | 用户计划下次去云南大理旅行，喜欢自由行，最远去过马尔代夫 | 云南大理, 自由行, 马尔代夫 |

---

## 14. 残缺文本与特殊边界

### 14.1 专名中含倾向词

如果 `喜欢、最爱、不想要` 等字样属于作品名、品牌名或昵称，必须保留实体本身。

- 作品名 `《最爱》` → `最爱`。
- 歌曲名 `《不想要离别》` 不能删成 `离别`。

### 14.2 details 截断或缺少对象

不得编造缺失对象。为保证非空，保留仍可确认的最小片段：

- `用户永远都喜欢` → `永远都喜欢`。
- `用户有一只拉布拉多犬，性格可爱，喜欢吃` → `拉布拉多犬, 可爱, 喜欢吃`。

### 14.3 疑问、请求与提醒

疑问或请求本身无画像价值时提取目标对象；提醒任务则保留对象、时间和提醒事件。

- `是否认识华为的白秀才` → `华为, 白秀才`。
- `每天早上7:00提醒父亲吃药` → `父亲, 每天早上7:00, 吃药提醒`。

---

## 15. 输出前强制语义校验

逐行检查：

1. 是否还残留只负责引出对象的 `看、听、吃、玩、观看、学习、收藏`？
2. 正向偏好是否已经压缩为对象或类别？
3. 否定、拒绝、没有、失败、无法等极性是否完整？
4. 社交关系是否漏掉 `朋友、同学、父亲、女儿` 等关系锚点？
5. 是否保留了无区分价值的 `一个、一只、一台`？
6. 是否保留了 `相关内容、综艺节目、的电影、的歌曲、问题、状态、习惯` 等外壳？
7. 人名、作品名、物品、地点是否错误粘连？
8. 地点层级、时间与事件、属性与对象是否被拆成孤立碎片？
9. 是否把具体专名改错、补造常识或改变数字？
10. 是否重复输出上位类别和具体实例？
11. `keywords` 是否非空，分隔符是否严格为 `, `？

切片级人工抽查至少覆盖：正向偏好、否定项、关系类、位置类、时间事件类、健康状态类各 3 行。发现同类错误两次及以上，必须回查整个切片，不得只修抽中行。

---

## 16. 子 Agent 输出格式

- 每个输入行必须处理，不遗漏、不抽样、不重新排序。
- `keywords` 不得为空。
- 保持 `id`、`topic`、`subtopic`、`details` 原样不变，只追加 `keywords`。
- 输出 UTF-8 CSV。
- 列顺序严格为 `id,topic,subtopic,details,keywords`。
- 多关键词使用英文逗号加一个空格：`, `。
- 不加序号、解释、置信度或备注列。

全量输出验收还包括：

- 数据行数以当轮 `meta.json` 为准。
- 最终列为 `user_id,topic,subtopic,details,keywords`。
- 原始前四列逐行保持一致。
- `keywords` 无空值。
- 最终文件使用 GB18030、CRLF、无 BOM。
- 多实体、否定项、关系项和依存组合必须人工抽查，不能只依赖正则统计。
